1. Распаковать архив vk-groups-api.zip
2. Указать токен для пользования API VK и актуальную версию VK API в файле .env
3. Выполнить команду docker-compose up -d --build для создания и запуска контейнеров
4. После создания контейнеров, API будет доступно по адресу 127.0.0.1:8008